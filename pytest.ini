[pytest]
testpaths = tests
pythonpath = backend
# Deliberately no -q here: the README tells reviewers to run `pytest tests/ -q`,
# and a -q in addopts would make that -qq, which silently hides the pass/fail
# summary line — the one thing they are running it to see.
filterwarnings =
    ignore::DeprecationWarning
